# ── MCP Proxy (src/app/api/mcp) & Ollama URL guard (src/app/api/chat) ──
# Set to "1" ONLY for local development to allow http:// URLs and
# private/localhost MCP servers through the SSRF-shielded proxy, and to
# allow local/private Ollama URLs on hosted deployments.
# NEVER enable this on a publicly deployed instance (e.g. Vercel).
ALLOW_PRIVATE_MCP=

# ── Rate Limiting (optional) ────────────────────────────────
# If both Upstash variables are empty, rate limiting is disabled.
# Create a free database at https://upstash.com -> Redis -> REST API details.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# Requests per minute per IP (defaults shown)
RATE_LIMIT_CHAT_PER_MINUTE=20
RATE_LIMIT_MCP_PER_MINUTE=60
